namespace ServiceStudio.View.DriftChats.DisplayModes {
    public class Open : DisplayMode {

        public Open(PlaybookId playbookId = PlaybookId.Default) {
            PlaybookId = playbookId;
        }
    }
}
